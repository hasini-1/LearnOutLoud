import type * as tf from '@tensorflow/tfjs-core';
import type { ConvParams } from './types';
export declare function loadConvParamsFactory(extractWeightEntry: <T extends tf.Tensor>(originalPath: string, paramRank: number) => T): (prefix: string) => ConvParams;
