import { FaceMatch } from '../classes/FaceMatch';
import { LabeledFaceDescriptors, type LabeledFaceDescriptorsJSON } from '../classes/LabeledFaceDescriptors';
import type { WithFaceDescriptor } from '../factories';
export interface FaceMatcherJSON {
    distanceThreshold: number;
    labeledDescriptors: LabeledFaceDescriptorsJSON[];
}
export declare class FaceMatcher {
    private _labeledDescriptors;
    private _distanceThreshold;
    constructor(inputs: LabeledFaceDescriptors | WithFaceDescriptor<object> | Float32Array | Array<LabeledFaceDescriptors | WithFaceDescriptor<object> | Float32Array>, distanceThreshold?: number);
    get labeledDescriptors(): LabeledFaceDescriptors[];
    get distanceThreshold(): number;
    computeMeanDistance(queryDescriptor: Float32Array, descriptors: Float32Array[]): number;
    matchDescriptor(queryDescriptor: Float32Array): FaceMatch;
    findBestMatch(queryDescriptor: Float32Array): FaceMatch;
    toJSON(): FaceMatcherJSON;
    static fromJSON(json: FaceMatcherJSON): FaceMatcher;
}
