"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.LabeledBox = void 0;
const utils_1 = require("../utils");
const Box_1 = require("./Box");
class LabeledBox extends Box_1.Box {
    static assertIsValidLabeledBox(box, callee) {
        Box_1.Box.assertIsValidBox(box, callee);
        const boxRecord = box;
        if (!(0, utils_1.isValidNumber)(boxRecord.label)) {
            throw new Error(`${callee} - expected property label (${boxRecord.label}) to be a number`);
        }
    }
    constructor(box, label) {
        super(box);
        this._label = label;
    }
    get label() {
        return this._label;
    }
}
exports.LabeledBox = LabeledBox;
