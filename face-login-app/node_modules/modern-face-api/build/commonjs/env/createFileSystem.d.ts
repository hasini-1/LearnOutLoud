import type { FileSystem } from './types';
interface NodeFS {
    readFile: (path: string, callback: (err: NodeJS.ErrnoException | null, data: Buffer) => void) => void;
}
export declare function createFileSystem(fs?: NodeFS): FileSystem;
export {};
