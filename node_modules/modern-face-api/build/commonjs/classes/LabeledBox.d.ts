import type { IBoundingBox } from './BoundingBox';
import { Box } from './Box';
import type { IRect } from './Rect';
export declare class LabeledBox extends Box<LabeledBox> {
    static assertIsValidLabeledBox(box: unknown, callee: string): void;
    private _label;
    constructor(box: IBoundingBox | IRect | unknown, label: number);
    get label(): number;
}
