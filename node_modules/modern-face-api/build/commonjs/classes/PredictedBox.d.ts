import type { IBoundingBox } from './BoundingBox';
import { LabeledBox } from './LabeledBox';
import type { IRect } from './Rect';
export declare class PredictedBox extends LabeledBox {
    static assertIsValidPredictedBox(box: unknown, callee: string): void;
    private _score;
    private _classScore;
    constructor(box: IBoundingBox | IRect | unknown, label: number, score: number, classScore: number);
    get score(): number;
    get classScore(): number;
}
