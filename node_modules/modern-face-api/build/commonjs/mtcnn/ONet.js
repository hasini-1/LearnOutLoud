"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ONet = ONet;
const tf = require("@tensorflow/tfjs-core");
const common_1 = require("../common");
const fullyConnectedLayer_1 = require("../common/fullyConnectedLayer");
const prelu_1 = require("./prelu");
const sharedLayers_1 = require("./sharedLayers");
function ONet(x, params) {
    return tf.tidy(() => {
        let out = (0, sharedLayers_1.sharedLayer)(x, params);
        out = tf.maxPool(out, [2, 2], [2, 2], 'same');
        out = (0, common_1.convLayer)(out, params.conv4, 'valid');
        out = (0, prelu_1.prelu)(out, params.prelu4_alpha);
        const vectorized = tf.reshape(out, [out.shape[0], params.fc1.weights.shape[0]]);
        const fc1 = (0, fullyConnectedLayer_1.fullyConnectedLayer)(vectorized, params.fc1);
        const prelu5 = (0, prelu_1.prelu)(fc1, params.prelu5_alpha);
        const fc2_1 = (0, fullyConnectedLayer_1.fullyConnectedLayer)(prelu5, params.fc2_1);
        const max = tf.expandDims(tf.max(fc2_1, 1), 1);
        const prob = tf.softmax(tf.sub(fc2_1, max), 1);
        const regions = (0, fullyConnectedLayer_1.fullyConnectedLayer)(prelu5, params.fc2_2);
        const points = (0, fullyConnectedLayer_1.fullyConnectedLayer)(prelu5, params.fc2_3);
        const scores = tf.unstack(prob, 1)[1];
        return { scores, regions, points };
    });
}
